import * as React from 'react';
import Routes from './routes';

interface IProps {
  
}

const App: React.FC<IProps> = (props: IProps) => {
  const {
    
  } = props;
  
  return (
    <>
      <Routes />
    </>
  );
}

export default App;
