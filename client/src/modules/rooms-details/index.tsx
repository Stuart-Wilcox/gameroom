import * as React from 'react';

interface IProps {
  
}

const RoomsDetails: React.FC<IProps> = (props: IProps) => {
  const {
    
  } = props;
  
  return (
    <>
      Rooms Details
    </>
  );
}

export default RoomsDetails;
