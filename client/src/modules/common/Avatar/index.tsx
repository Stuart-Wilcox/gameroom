import styled from "styled-components";
import { Avatar } from '@material-ui/core';
import { AvatarGroup as AG } from '@material-ui/lab';

export default styled(Avatar)`

`;

export const AvatarGroup = styled(AG)`

`;
