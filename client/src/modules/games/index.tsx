import * as React from 'react';

interface IProps {
  
}

const GamesPage: React.FC<IProps> = (props: IProps) => {
  const {
    
  } = props;
  
  return (
    <>
      Games Page
    </>
  );
}

export default GamesPage;
